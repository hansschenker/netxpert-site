# Debugging

Seeing inside streams — techniques for understanding what a pipeline actually does.

Topics that belong here: `tap` as a window into streams, writing a `debug` operator, common silent failures (forgotten subscribe, swallowed errors, completed sources), and reading RxJS stack traces.

## Topics

*No topics yet — this section grows as we explore.*
