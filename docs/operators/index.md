# Operators

Pipeable functions that transform streams — the vocabulary of RxJS.

Topics that belong here, grouped by category: transformation (`map`, `scan`), filtering (`filter`, `debounceTime`, `distinctUntilChanged`), flattening (`switchMap`, `mergeMap`, `concatMap`, `exhaustMap`), combination (`combineLatest`, `merge`, `zip`, `withLatestFrom`), error handling (`catchError`, `retry`), and utility (`tap`, `delay`).

## Topics

*No topics yet — this section grows as we explore.*
