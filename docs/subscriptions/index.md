# Subscriptions

The handle on a running stream — and how to clean it up.

Topics that belong here: teardown logic, `unsubscribe`, composing subscriptions with `add`, unsubscribe strategies (`takeUntil`, `take(1)`, manual), subscription leaks and how to spot them.

## Topics

*No topics yet — this section grows as we explore.*
