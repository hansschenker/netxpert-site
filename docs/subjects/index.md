# Subjects

Both observable and observer — the bridge into multicasting.

Topics that belong here: `Subject`, `BehaviorSubject`, `ReplaySubject`, `AsyncSubject`, choosing between them, multicasting with `share` / `shareReplay`, and subjects as event buses vs state holders.

## Topics

*No topics yet — this section grows as we explore.*
