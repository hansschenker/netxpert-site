# TypeScript

Typing reactive code well — making the compiler work for your streams.

Topics that belong here: generics in operators and observables, type inference through `pipe`, narrowing with `filter` type guards, typing custom operators, discriminated unions in streams, and strictness pitfalls.

## Topics

*No topics yet — this section grows as we explore.*
