# Custom Operators

Building your own operators — the step from using RxJS to extending it.

Topics that belong here: operators as functions `(source: Observable<A>) => Observable<B>`, composing existing operators with `pipe`, writing operators from scratch with `new Observable`, and packaging reusable operator libraries.

## Topics

*No topics yet — this section grows as we explore.*
