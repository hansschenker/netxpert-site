# Observers

The consumer side of a stream: the object that receives notifications.

Topics that belong here: the `next` / `error` / `complete` contract, partial observers, observer vs callback styles of `subscribe`, and how the observable–observer relationship defines push-based programming.

## Topics

*No topics yet — this section grows as we explore.*
