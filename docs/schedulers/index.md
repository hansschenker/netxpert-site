# Schedulers

Controlling *when* and *in what execution context* a stream does its work.

Topics that belong here: `asyncScheduler`, `asapScheduler`, `queueScheduler`, `animationFrameScheduler`, `observeOn` vs `subscribeOn`, and when schedulers actually matter in application code.

## Topics

*No topics yet — this section grows as we explore.*
